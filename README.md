# Chart Labeling Instructions

This project contains a simple line chart in `class12.py`.

## What students should do

Open `class12.py` and add the following lines before `plt.show()`:


```

Save the file and run it again to see the updated chart.